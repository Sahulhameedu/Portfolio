{
  "buildCommand": "flutter build web --release",
  "outputDirectory": "build/web",
  "env": [
    "EMAIL_SERVICE_ID",
    "EMAIL_TEMPLATE_ID",
    "EMAIL_PUBLIC_KEY",
    "EMAIL_PRIVATE_KEY"
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
